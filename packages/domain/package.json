{
  "name": "@clinica/domain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./rules": "./src/rules.ts",
    "./index": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@clinica/types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
